spring:
  application:
    name: jogosapi
  datasource:
    url: jdbc:postgresql://localhost:5432/jogos
    username: postgres
    password: bergs
    driver-class-name: org.postgresql.Driver
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: none
      properties:
        hibernate.format.sql: true
